# ---- API server ----
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9
pydantic>=2.7
httpx>=0.27
truststore>=0.9          # trust the OS certificate store for all HTTPS research calls

# ---- Document reading / writing (PDF, DOCX, Markdown) ----
pymupdf>=1.24            # primary PDF text + table extraction
pypdf>=4.2               # fallback PDF reader
pdfplumber>=0.11         # fallback table extraction
python-docx>=1.1         # DOCX read + write
reportlab>=4.2           # enterprise PDF publishing
markdown-it-py>=3.0      # Markdown parsing

# ---- Retrieval (RAG) ----
numpy>=1.26
scikit-learn>=1.5        # TF-IDF vectors, cosine similarity
rank-bm25>=0.2.2         # lexical BM25 ranking
rapidfuzz>=3.9           # fuzzy sentence matching for the originality guard

# ---- Online research (each source has an httpx fallback) ----
arxiv>=2.1               # physics, CS, maths, quant-bio, econ
pyalex>=0.14             # OpenAlex: every discipline
habanero>=1.2            # Crossref: DOI metadata, every discipline
semanticscholar>=0.8     # Semantic Scholar graph
biopython>=1.84          # PubMed / NCBI Entrez: life & health sciences
wikipedia-api>=0.7       # encyclopaedic background
ddgs>=6.0                # DuckDuckGo web search
trafilatura>=1.12        # clean article text from web pages
beautifulsoup4>=4.12     # HTML fallback parser
lxml>=5.2
feedparser>=6.0          # RSS / Atom feeds (arXiv fallback)
wbgapi>=1.0              # World Bank open data (numeric indicators)

# ---- Data analysis & figures ----
pandas>=2.2
scipy>=1.13
statsmodels>=0.14
matplotlib>=3.9
openpyxl>=3.1            # user-supplied .xlsx datasets
textstat>=0.7            # readability metrics for the style pass
